<krpano>
	
	<events name="map__events" keep="true"
		
		onresize="
			callwith(layer[map__container], resize);
		"
		onclick="
			if (layer[map__container].state == 2,
				callwith(layer[map__expand], onclick_actions);
			);
		"
	/>
	
</krpano>